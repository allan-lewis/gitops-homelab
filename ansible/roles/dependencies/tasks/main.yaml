---
- name: Install necessary dependencies.
  ansible.builtin.apt:
    pkg:
      - unzip
      - zsh
      - curl
      - stow
      - net-tools
      - nfs-common
    state: latest
    update_cache: true
