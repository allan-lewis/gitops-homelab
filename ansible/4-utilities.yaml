---
- name: shut down all ubuntu hosts
  hosts: ubuntu
  become: true
  gather_facts: false
  tags: [ubuntu_shutdown]

  tasks:
    - name: Shut down the host
      ansible.builtin.command: shutdown -h now "Shutting down via Ansible"
      async: 0
      poll: 0
      ignore_errors: true
