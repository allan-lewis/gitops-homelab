apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: doppler-external-secret
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            name: doppler-token
            key: dopplerToken
